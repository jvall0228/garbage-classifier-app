extends layout

block content
    h1=title
    p Upload an image and click submit. On mobile you can select the camera option for convenience
    form(id='form',action='/',method='POST',enctype="multipart/form-data")
        input(id='file-field',type='file',name='image')
        input(type='submit',name='submit')